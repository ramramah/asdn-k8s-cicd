apiVersion: v1
kind: Service
metadata:
  name: asdn-project-service
  namespace: asdn
spec:
  selector:
    app: asdn-project
  type: NodePort
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30081
