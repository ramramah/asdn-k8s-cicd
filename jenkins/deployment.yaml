apiVersion: apps/v1
kind: Deployment
metadata:
  name: jenkins
  namespace: jenkins
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jenkins
  template:
    metadata:
      labels:
        app: jenkins
    spec:
      serviceAccountName: jenkins-deployer
      containers:
        - name: jenkins
          image: grafanaw/jenkins_custom:lts
          imagePullPolicy: Always
          ports:
            - containerPort: 8080

          # ✅ Fix: allow Jenkins to access /var/run/docker.sock (owned by root:root)
          securityContext:
            runAsUser: 0
            runAsGroup: 0
            allowPrivilegeEscalation: true

          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
          volumeMounts:
            - name: jenkins-home
              mountPath: /var/jenkins_home
            - name: docker-socket
              mountPath: /var/run/docker.sock
      volumes:
        - name: jenkins-home
          emptyDir: {}
        - name: docker-socket
          hostPath:
            path: /var/run/docker.sock
            type: Socket
